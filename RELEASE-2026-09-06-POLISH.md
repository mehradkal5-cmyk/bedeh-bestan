# پولیش فرم‌ها، تم و دعوت اختصاصی دنگ

- نام و مبلغ هر نفر در ردیف جدا، افزودن/حذف نفر، جابه‌جایی با Enter و تقسیم مساوی به انتخاب سازنده. جمع سهم‌ها، نام تکراری و مبلغ مثبت کنترل می‌شوند.
- جداکنندهٔ هزارگان هنگام تایپ مبلغ، با ارقام فارسی و حفظ محل تایپ.
- کنترل تم در تنظیمات و نوار بالا؛ تغییر با View Transition در مرورگر پشتیبان، اصلاح رنگ متن/وضعیت/ورودی‌ها و رعایت کاهش حرکت. حرکت کوتاه ورود پنجره و جابه‌جایی صفحه.
- نوار نکته‌ها در پوستهٔ ثابت می‌ماند؛ جابه‌جایی صفحه و همگام‌سازی آن را دوباره نمی‌سازد.
- صدای ملایم اعلان تازه پس از تعامل با صفحه، با تنظیم خاموش/روشن محلی؛ بارگیری اولیه اعلان‌های قدیمی صدا ندارد.
- نشست ورود در همان مرورگر باقی می‌ماند؛ تمدید نشست در تب‌ها با Web Locks هماهنگ است. خروج، پاک‌کردن دادهٔ مرورگر یا لغو نشست سرور همچنان ورود دوباره می‌خواهد. حالت خصوصی مرورگر تضمین ماندگاری ندارد.

## سهم متعلق به همان فرد

لینک دنگ همچنان مشترک است. سازنده از جزئیات هر سهم «کد دعوت» می‌سازد و آن را خصوصی به فرد می‌دهد. کد ۸۰ بیتی تصادفی تنها یک‌بار نمایش داده می‌شود؛ فقط SHA-256 آن در جدول مستقل با RLS و بدون مجوز مشتری ذخیره می‌شود. ساخت دوباره، کد قبلی را عوض می‌کند؛ تا تصمیم دربارهٔ درخواستِ دارای کد، تعویض مسدود است.

سرور کد را پیش از ایجاد درخواست عضویت بررسی می‌کند. تأیید سازنده همچنان لازم است و آدرس حساب درخواست‌کننده در داشبورد سازنده دیده می‌شود. آدرس حساب یا داشتن کد، تأیید هویت واقعی نیست؛ سازنده باید حساب را با فرد تطبیق دهد. به‌اشتراک‌گذاری عمدی کد توسط گیرنده خارج از تضمین این روش است.

عضویت‌های پذیرفته‌شده حفظ شده‌اند. درخواست قدیمی بدون کد حذف نمی‌شود؛ برای تأیید، فرد باید لینک را دوباره با کد سهم باز کند. شناسهٔ همان درخواست حفظ می‌شود. API قدیمی امانت/قرض و دسترسی اعضا پابرجاست؛ نسخهٔ قدیمی نمی‌تواند شرط کد دنگ را دور بزند.

## انتشار و شواهد

Migration `20260906004000_personal_share_codes.sql` و توابع `claim-share-link` و `record-command` پیش از فرانت‌اند در پروژهٔ `vyjdcjpnlvklkvbxonth` منتشر شدند. `supabase db lint` هیچ خطای schema گزارش نکرد. بررسی نحو همهٔ فایل‌های جاوااسکریپت، بررسی نوع Deno دو تابع و build انتشار موفق بود. بازبینی ایستای UI تنها نشان رنگی اعلان خوانده‌نشده را علامت زد؛ چون معنای مشخص دارد حفظ شد.

طبق درخواست کاربر **هیچ تست خودکار، دستی یا مرورگری اجرا نشده است**. صحت رفتاری ورود مجدد، چند تب هم‌زمان، کد صحیح/غلط و رقابت درخواست‌ها، صدا، کنتراست رندرشده و محسوس‌بودن انیمیشن در دستگاه واقعی آزمایش نشده‌اند. بررسی فایل آنلاین صرفاً تطبیق artifact انتشار است.

Push دستگاه و ثبت کارت کامل همچنان منتظر تنظیمات خارجی ذکرشده در `RELEASE-2026-09-06.md` هستند؛ رد قبلیِ ثبت کلیدهای سرور دور زده یا دوباره اجرا نشده است. صدای داخل برنامه مستقل از Push است.
