# یکپارچه‌سازی حساب و درخواست‌ها

دعوت پس از ورود در سرور بررسی می‌شود. گیرندهٔ پذیرفته‌شده و سازنده از یک
داشبورد استفاده می‌کنند. لغو دعوت، عضویت پذیرفته‌شده را حذف نمی‌کند.
دنگ‌ها نام و مبلغ سهم مستقل دارند؛ درخواست انتخاب سهم با تأیید سازنده متصل می‌شود.

درخواست‌های تمدید، بازگشت و پرداخت شناسه و تصمیم مستقل دارند. نوشتن عضویت،
رویداد، تصمیم و اعلان در تراکنش PostgreSQL انجام می‌شود. قفل رکورد و شناسهٔ
تکرار، اعمال هم‌زمان و دوبارهٔ تصمیم را کنترل می‌کنند. RLS و مجوزهای جدول
نوشتن مستقیم این مقادیر توسط مرورگر را محدود می‌کنند.

Realtime فقط INSERT/UPDATE را با نشست کاربر و RLS دریافت می‌کند. بازیابی
هنگام اتصال مجدد/بازشدن برنامه و polling با فاصلهٔ افزایشی وجود دارد. فرم‌ها
بیرون از ناحیهٔ به‌روزرسانی قرار دارند. اعلان‌های خوانده‌شده، تعداد خوانده‌نشده
و ترجیحات یادآوری از دیتابیس دریافت می‌شوند؛ خطا جایگزین فهرست خالی نمی‌شود.

## انتشار و اعتبارسنجی

- migration رسیدها که روی سرور جا مانده بود، پیش از تغییرات جدید اعمال شد.
- migrationهای عضویت، درخواست‌ها، صف Push و یادآوری پیش از فرانت‌اند منتشر شدند.
- ۱۱ Edge Function با کنترل ورود داخل برنامه یا راز زمان‌بندی منتشر شدند.
- بررسی نحوی JavaScript، بررسی نوع‌های Deno و build انتشار انجام شده است.
- بررسی نحوی/نوعی SQL با `supabase db lint` انجام شده است.
- طبق درخواست کاربر، هیچ تست خودکار، تست مرورگر یا سناریوی دستی اجرا نشده است.
  رفتار چندحسابی، رقابت درخواست‌ها، قطع‌و‌وصل Realtime، حفظ فرم و تحویل Push
  در اجرا آزمایش نشده‌اند. عبور build معادل تأیید این رفتارها نیست.

## مرحلهٔ خارجی باقیمانده

بازبینی خودکار مجوز، ثبت کلیدهای تازه در پروژهٔ `vyjdcjpnlvklkvbxonth` را
رد کرد. کلیدهای زیر هنوز روی سرور ثبت نشده‌اند:

- `VAPID_PUBLIC_KEY`، `VAPID_PRIVATE_KEY`، `VAPID_SUBJECT`
- `CARD_ENCRYPTION_KEY`
- `REMINDER_CRON_SECRET`

در نتیجه Push دستگاه فعال اعلام نمی‌شود و ثبت کارت کامل تا تنظیم کلید
رمزنگاری پیام خطای مشخص دارد. هیچ کارت رمز‌شده‌ای در بررسی پیش از انتشار
وجود نداشت؛ کلید موجودی جایگزین نشده است. مقادیر خصوصی وارد Git یا dist نشده‌اند.
پس از تأیید این مرحله، راز cron باید در Vault نیز ثبت شود و `push-dispatch`
هر دقیقه فراخوانی شود. موفقیت فقط بر اساس پذیرش سرویس Push ثبت می‌شود؛
نبود کلید صف را به‌اشتباه موفق نمی‌کند.

یادآوری **داخل برنامه** به راز خارجی نیاز ندارد: job دیتابیس
`bedeh-bestan-in-app-reminders` ساعت ۰۵:۳۰ UTC، برابر با ۹ صبح تهران،
`create_due_reminders()` را اجرا می‌کند. ترجیحات هر طرف جداگانه بررسی می‌شود.

دادهٔ قدیمی دنگ حفظ شده و پیش از دعوت نیازمند تعیین صریح سهم‌هاست. اگر یک
نصب دیگر دنگ قدیمی دارای پرداخت داشته باشد، سهم‌بندی خودکار آن مسدود می‌شود
تا پرداخت‌ها بدون تصمیم صریح به اشخاص نسبت داده نشوند؛ در این پروژه هنگام
بررسی هیچ پرداخت قدیمی وجود نداشت.

مرجع پیاده‌سازی: [Supabase Realtime و RLS](https://supabase.com/docs/guides/realtime/postgres-changes)
و [web-push](https://github.com/web-push-libs/web-push).
