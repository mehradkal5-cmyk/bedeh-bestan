# Supabase backend integration

- [x] Define persistent schema and enable RLS on every application table.
- [x] Add Edge Functions for authenticated commands, share access, recipient actions, card access, and reminders.
- [x] Keep card encryption and all privileged keys server-side.
- [x] Add browser authentication/configuration gate so no browser record cache is used as the data source.
- [x] Replace the gate's iconography with Phosphor outline icons and accessible labels.
- [ ] Apply the migration and deploy functions to the user's Supabase project (requires credentials/project link).
- [ ] Exercise end-to-end database checks with two real test accounts after deployment.
