# بررسی ارسال ایمیل تأیید

## آنچه در کد اصلاح شده

- ثبت‌نام و ارسال دوباره، `redirect_to` را به Supabase Auth می‌فرستند.
- پاسخ کاربر بدون توکن، ورود موفق محسوب نمی‌شود.
- وضعیت انتظار تأیید و ارسال دوباره در تنظیمات در دسترس است.
- کد و وضعیت HTTP خطای سرور حفظ می‌شود؛ موفقیت درخواست به معنی تحویل ایمیل نیست.

## بررسی لازم در سرویس واقعی

1. وارد [تنظیمات SMTP پروژه](https://supabase.com/dashboard/project/vyjdcjpnlvklkvbxonth/auth/smtp) شوید.
2. برای ارسال به کاربران عمومی، SMTP اختصاصی با فرستنده و دامنهٔ تأییدشده تنظیم کنید. اطلاعات ورود SMTP را فقط در داشبورد وارد کنید؛ نه در `runtime-config.js` یا فایل‌های فرانت‌اند.
3. در Auth → URL Configuration، مقدار Site URL و تنها ورودی Redirect URLs با تأیید کاربر روی `http://127.0.0.1:4173/` تنظیم شده‌اند و پس از بارگذاری دوبارهٔ داشبورد بررسی شدند. در انتشار عمومی، دامنهٔ واقعی HTTPS را جایگزین کنید.
4. قالب Confirm signup باید از `{{ .ConfirmationURL }}` استفاده کند و نباید لینک ثابت یا دامنهٔ قدیمی داشته باشد.
5. یک ثبت‌نام یا ارسال دوبارهٔ مجاز انجام دهید؛ پاسخ درخواست و Auth Logs، سپس گزارش تحویل ارائه‌دهندهٔ ایمیل را بررسی کنید. خطای واقعی را مبنای تشخیص قرار دهید.
6. لینک دریافتی را باز کنید؛ فقط پس از دریافت نشست معتبر، ورود باید موفق شود. دکمهٔ مدیریت حساب در تنظیمات باقی می‌ماند.

SMTP پیش‌فرض Supabase برای محیط تولید نیست: تنها به نشانی اعضای تیم ارسال می‌کند و محدودیت نرخ سخت‌گیرانه دارد. این یک محدودیت مستند سرویس است، نه اثبات اینکه تنظیم فعلی این پروژه علت خطاست.

منبع: [راهنمای رسمی SMTP](https://supabase.com/docs/guides/auth/auth-smtp)

## وضعیت تأیید این تغییر

۴۰ تست محلی با پاسخ‌های قراردادی Auth و آزمون‌های تاریخ/رابط موفق شدند؛ عامل هیچ حساب آزمایشی روی سرویس واقعی نساخت و تحویل ایمیل تأیید نشده است. درخواست مستقیم شبکه در محیط اجرا با `EACCES` مسدود شد، اما داشبورد واقعی از نشست واردشدهٔ Chrome قابل بررسی بود.

مشاهدات مستقیم داشبورد در ۲۰۲۶-۰۹-۰۲:

- `Enable custom SMTP` خاموش است.
- پیش از اصلاح، Site URL برابر `http://localhost:3000` و Redirect URLs خالی بود. با تأیید کاربر، Site URL به `http://127.0.0.1:4173/` تغییر کرد و همین آدرس به Redirect URLs اضافه شد؛ ماندگاری هر دو پس از بارگذاری دوبارهٔ داشبورد تأیید شد.
- درخواست‌های اخیر `POST /signup` با HTTP 200 و پیام `user repeated signup: request completed` ثبت شده‌اند. این‌ها اثبات ارسال ایمیل تازه نیستند.
- درخواست‌های ورود `POST /token` با HTTP 400 و پیام `Invalid login credentials` رد شده‌اند.
- لینک قبلی در `GET /verify` با HTTP 403 و پیام `One-time token not found` رد شده است.
- یک درخواست `GET /user` نیز HTTP 403 با `token is expired` داشته است.

تنها تغییرات خارجی این مرحله، Site URL و افزودن Redirect URL تأییدشده بودند. SMTP تغییر نکرد و تحویل ایمیل همچنان تأیید نشده است. SMTP اختصاصی به انتخاب ارائه‌دهنده و واردکردن اطلاعات محرمانه در داشبورد نیاز دارد.

### تکمیل رسیدگی به خطاهای مشاهده‌شده

- نشست منقضی‌شده از مسیر استاندارد `grant_type=refresh_token` تمدید می‌شود؛ درخواست‌های هم‌زمان یک عملیات تمدید مشترک دارند.
- خطای موقت شبکه یا 5xx دیگر توکن‌ها را پاک نمی‌کند. ابطال قطعی نشست همچنان دسترسی را قطع می‌کند.
- پاسخ دیررس تمدید، بعد از خروج کاربر نمی‌تواند نشست را دوباره برقرار کند.
- callback نامعتبر، پارامترهای خطا را از URL حذف و فرم درخواست لینک تازه را نشان می‌دهد.
- فرم بازیابی در عرض ۳۷۵ پیکسل تصویری بررسی شد: بدون اسکرول افقی، دکمه‌های حداقل ۴۴ پیکسل و خطای ایمیل در همان فرم؛ هیچ ایمیلی در این آزمون ارسال نشد.
- پس از دریافت تأیید کاربر و ذخیره، وضعیت URL Configuration دوباره خوانده شد: Site URL برابر `http://127.0.0.1:4173/` و فهرست Redirect URLs شامل تنها همین آدرس بود.
